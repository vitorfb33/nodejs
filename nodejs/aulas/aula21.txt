POO

Pilares:
- Abstração
- Herança
- Polimorfismo
- Encapsulamento

//Abstração
Classe modelo - objeto

//Herança 
 Extende(reutiliza) atributos e metodos da classe principal



//Polimorfismo
Modifica o comportamento de um método existente na classe principal



//Encapsulamento
segurança (protege o acesso as variaveis)
-- atributos(variaveis) --> privados
--metodos especificos (GET e SET) são  usados para acessar as variaveis